apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kctl-vpc2
spec:
  storageClassName: ibmc-vpc-block-5iops-tier
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi